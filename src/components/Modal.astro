---
interface Props {
  id: string;
  title: string;
  maxWidth?: 'sm' | 'md' | 'lg' | 'xl' | '2xl' | '4xl';
}

const { id, title, maxWidth = 'lg' } = Astro.props;

const maxWidthClasses = {
  sm: 'max-w-sm',
  md: 'max-w-md',
  lg: 'max-w-lg',
  xl: 'max-w-xl',
  '2xl': 'max-w-2xl',
  '4xl': 'max-w-4xl',
};
---

<div
  data-modal={id}
  aria-hidden="true"
  aria-labelledby={`${id}-title`}
  role="dialog"
  aria-modal="true"
  class="modal fixed inset-0 z-50 hidden items-center justify-center p-4"
>
  <!-- Overlay -->
  <div
    data-modal-overlay
    class="fixed inset-0 bg-gray-900/80 transition-opacity duration-300"
  >
  </div>

  <!-- Modal Content -->
  <div
    class={`modal-content relative z-10 w-full ${maxWidthClasses[maxWidth]} transform rounded-lg bg-white shadow-2xl transition-all duration-300 ease-in-out`}
  >
    <!-- Header -->
    <div
      class="flex items-start justify-between border-b border-gray-200 px-6 py-4"
    >
      <h2
        id={`${id}-title`}
        class="text-center text-2xl font-bold text-gray-900 md:text-3xl"
      >
        {title}
      </h2>
      <button
        type="button"
        data-modal-close
        aria-label="Close modal"
        class="rounded-full p-2 text-gray-400 transition-colors hover:bg-gray-100 hover:text-gray-600 focus:outline-none focus-visible:ring-2 focus-visible:ring-blue-900 focus-visible:ring-offset-2"
      >
        <!-- X icon -->
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
          class="h-6 w-6"
        >
          <line x1="18" y1="6" x2="6" y2="18"></line>
          <line x1="6" y1="6" x2="18" y2="18"></line>
        </svg>
      </button>
    </div>

    <!-- Body -->
    <div class="px-8 py-8">
      <slot />
    </div>
  </div>
</div>

<script>
  // Modal functionality
  function initModal() {
    const modals = document.querySelectorAll('[data-modal]');

    modals.forEach((modal) => {
      const modalElement = modal as HTMLElement;
      const closeBtn = modal.querySelector('[data-modal-close]');
      const overlay = modal.querySelector('[data-modal-overlay]');
      let triggerElement: HTMLElement | null = null;

      // Close modal function
      const closeModal = () => {
        modalElement.classList.add('hidden');
        modalElement.setAttribute('aria-hidden', 'true');

        // Restore body scroll
        document.body.style.overflow = '';
        document.body.style.position = '';
        document.body.style.width = '';
        document.body.style.top = '';

        // Restore scroll position (for iOS Safari)
        const scrollY = modalElement.dataset.scrollY;
        if (scrollY) {
          window.scrollTo(0, parseInt(scrollY));
          delete modalElement.dataset.scrollY;
        }

        // Return focus to trigger button
        if (triggerElement) {
          triggerElement.focus();
          triggerElement = null;
        }
      };

      // Close on X button click
      closeBtn?.addEventListener('click', closeModal);

      // Close on overlay click (only backdrop, not modal content)
      overlay?.addEventListener('click', (e) => {
        if (e.target === overlay) {
          closeModal();
        }
      });

      // Close on Escape key
      const handleKeyDown = (e: KeyboardEvent) => {
        if (e.key === 'Escape' && !modalElement.classList.contains('hidden')) {
          closeModal();
        }
      };

      document.addEventListener('keydown', handleKeyDown);

      // Store trigger element for focus restoration
      const openModal = (trigger: HTMLElement) => {
        triggerElement = trigger;

        // Store current scroll position for iOS Safari
        modalElement.dataset.scrollY = window.scrollY.toString();

        // Prevent body scroll
        document.body.style.overflow = 'hidden';

        // iOS Safari scroll lock
        document.body.style.position = 'fixed';
        document.body.style.width = '100%';
        document.body.style.top = `-${window.scrollY}px`;

        // Show modal
        modalElement.classList.remove('hidden');
        modalElement.setAttribute('aria-hidden', 'false');

        // Focus first focusable element
        setTimeout(() => {
          const firstFocusable = modalElement.querySelector(
            'button:not([disabled]), a[href]'
          ) as HTMLElement;
          firstFocusable?.focus();
        }, 10);
      };

      // Expose open function for external triggers
      (modalElement as any)._openModal = openModal;
    });
  }

  // Initialize on page load
  document.addEventListener('astro:page-load', initModal);
</script>

<style>
  /* Modal display when not hidden */
  .modal:not(.hidden) {
    display: flex;
  }

  /* Modal content entrance animation */
  .modal.hidden .modal-content {
    transform: scale(0.95);
    opacity: 0;
  }

  .modal:not(.hidden) .modal-content {
    transform: scale(1);
    opacity: 1;
  }

  /* Smooth transitions */
  .modal-content {
    transition:
      transform 0.3s ease-in-out,
      opacity 0.3s ease-in-out;
  }
</style>
