---
// Partners.astro - Partners section showcasing FiNAN's partnerships
import arcadaLogo from '../assets/images/partners/arcada-logo.svg';
import euResearchLogo from '../assets/images/partners/eu-research-innovation.svg';
import fdnLogo from '../assets/images/partners/filipino-nursing-diaspora-network.svg';
import finnishGovtEmblem from '../assets/images/partners/finnish_government_emblem.svg';
import ianpcLogo from '../assets/images/partners/ianpc.svg';
import moniheliLogo from '../assets/images/partners/moniheli-logo.svg';
import oamkLogo from '../assets/images/partners/oamk-logo.svg';
import siennaLogo from '../assets/images/partners/sienna-logo.svg';
import wsdnLogo from '../assets/images/partners/wsdn.svg';
---

<section class="bg-white py-16 md:py-24">
  <div class="mx-auto max-w-7xl px-6">
    <!-- Header -->
    <div class="motion-fade-up mb-16 text-center">
      <h2 class="mb-4 text-3xl font-bold text-gray-900 md:text-4xl">
        Our Partners in Healthcare, Education & Policy
      </h2>
    </div>

    <!-- Partner Logos Grid -->
    <div
      class="motion-fade-up flex flex-col items-center gap-8 md:flex-row md:flex-wrap md:justify-center md:gap-12"
    >
      <div class="flex items-center justify-center px-4">
        <img
          src={finnishGovtEmblem.src}
          width={finnishGovtEmblem.width}
          height={finnishGovtEmblem.height}
          alt="Finnish Government"
          class="h-20 w-auto object-contain md:h-25"
          loading="lazy"
          decoding="async"
        />
      </div>

      <!-- OAMK -->
      <div class="flex items-center justify-center px-4">
        <img
          src={oamkLogo.src}
          width={oamkLogo.width}
          height={oamkLogo.height}
          alt="Oulu University of Applied Sciences"
          class="h-14 w-auto object-contain md:h-18"
          loading="lazy"
          decoding="async"
        />
      </div>

      <!-- Arcada -->
      <div class="flex items-center justify-center px-4">
        <img
          src={arcadaLogo.src}
          width={arcadaLogo.width}
          height={arcadaLogo.height}
          alt="Arcada University of Applied Sciences"
          class="h-20 w-auto object-contain md:h-26"
          loading="lazy"
          decoding="async"
        />
      </div>

      <!-- EU Research -->
      <div class="flex items-center justify-center px-4">
        <img
          src={euResearchLogo.src}
          width={euResearchLogo.width}
          height={euResearchLogo.height}
          alt="European Union Research and Innovation"
          class="h-24 w-auto object-contain md:h-35"
          loading="lazy"
          decoding="async"
        />
      </div>

      <!-- Moniheli -->
      <div class="flex items-center justify-center px-4">
        <img
          src={moniheliLogo.src}
          width={moniheliLogo.width}
          height={moniheliLogo.height}
          alt="Moniheli"
          class="h-7 w-auto object-contain md:h-9"
          loading="lazy"
          decoding="async"
        />
      </div>

      <!-- Filipino Nursing Diaspora Network -->
      <div class="flex items-center justify-center px-4">
        <img
          src={fdnLogo.src}
          width={fdnLogo.width}
          height={fdnLogo.height}
          alt="Filipino Nursing Diaspora Network"
          class="h-14 w-auto object-contain md:h-18"
          loading="lazy"
          decoding="async"
        />
      </div>

      <!-- SIENNA -->
      <div class="flex items-center justify-center px-4">
        <img
          src={siennaLogo.src}
          width={siennaLogo.width}
          height={siennaLogo.height}
          alt="Society of Internationally Educated Nurses in North America"
          class="h-20 w-auto object-contain md:h-24"
          loading="lazy"
          decoding="async"
        />
      </div>

      <!-- IANPC -->
      <div class="flex items-center justify-center px-4">
        <img
          src={ianpcLogo.src}
          width={ianpcLogo.width}
          height={ianpcLogo.height}
          alt="International Assembly for National Nursing Associations"
          class="h-20 w-auto object-contain md:h-24"
          loading="lazy"
          decoding="async"
        />
      </div>

      <!-- WSDN -->
      <div class="flex items-center justify-center px-4">
        <img
          src={wsdnLogo.src}
          width={wsdnLogo.width}
          height={wsdnLogo.height}
          alt="World Sustainable Development Network"
          class="h-12 w-auto object-contain md:h-16"
          loading="lazy"
          decoding="async"
        />
      </div>
    </div>
  </div>
</section>
