# Cloudflare Pages Redirect Rules
#
# NOTE:
# Host/domain-level redirects (e.g. www <-> apex, pages.dev -> custom domain)
# must be configured in Cloudflare dashboard Redirect Rules, not in this file.
# The `_redirects` file supports path-based redirects only.
