# Changelog - 2025-11-03 02:17 (Europe/Helsinki)

## Added

### FAQ Page Data-Driven Architecture

- **Created structured FAQ data source** (`src/data/pages/faq/faqData.ts`)
  - Added TypeScript interface `FAQ` for type-safe FAQ items
  - Migrated all 7 FAQ questions and answers from markdown to TypeScript
  - Exported `faqIntro` text for consistent page description
  - Implemented `as const` pattern for immutable data

### SEO Schema Markup Implementation

- **Added FAQ Schema (JSON-LD) for search engine optimization**
  - Created `FAQSchemaItem` interface for schema type safety
  - Implemented `generateFAQSchema()` function that converts FAQ data to JSON-LD format
  - Follows Google's FAQPage structured data specification
  - Enables rich snippets in search results and voice search optimization

- **Extended Layout component with head slot**
  - Added `<slot name="head" />` to `src/layouts/Layout.astro` (line 79)
  - Allows pages to inject custom head content (scripts, meta tags, etc.)
  - Enables page-specific SEO enhancements

## Changed

### FAQ Page Refactoring

- **Converted FAQ page to use semantic HTML**
  - Replaced manual numbering with proper `<ol>` ordered list structure
  - Changed from `<div>` elements with manual `{index + 1}.` to `<li>` elements
  - Browser now automatically handles numbering for better accessibility

- **Updated FAQ page to use data-driven approach**
  - Imports FAQ data from centralized TypeScript file
  - Dynamically renders all FAQs using `.map()` function
  - Maintains consistent styling with responsive typography

- **Improved FAQ page styling**
  - Added large, bold list markers (`marker:text-2xl marker:font-semibold`)
  - Maintained border separators between FAQ items
  - Preserved responsive design (text scales on mobile/desktop)

## Technical Details

### Files Modified

1. `src/data/pages/faq/faqData.ts` - Created new data file with interfaces and schema function
2. `src/pages/faq.astro` - Refactored to use data-driven approach with semantic HTML
3. `src/layouts/Layout.astro` - Added head slot for custom page scripts

### SEO Benefits

- Search engines can now display FAQs as rich snippets
- Improved voice search compatibility (e.g., "Hey Google, is FiNAN a recruitment agency?")
- Enhanced SERP (Search Engine Results Page) display
- Type-safe schema generation at build time

### Build Verification

- Production build successful
- Schema markup verified in `/dist/faq/index.html`
- All 7 FAQs properly structured with Question/Answer format
- Code formatted with ESLint and Prettier

## Notes

The FAQ page now follows best practices for:

- **Maintainability**: Single source of truth for FAQ content
- **Accessibility**: Semantic HTML with proper ordered list structure
- **SEO**: Google-compliant FAQ schema markup for rich snippets
- **Type Safety**: Full TypeScript support with interfaces
- **Consistency**: Centralized data structure for easy updates
