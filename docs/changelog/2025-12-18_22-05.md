# Changelog - 2025-12-18 22:05

## Fixed

### Favicon Implementation

- **Replaced all favicon files** with RealFaviconGenerator output to fix validation issues
- **Added SVG favicon support** (`favicon.svg`) for modern browsers
- **Added 96×96 PNG favicon** (`favicon-96x96.png`) for high-DPI desktop displays
- **Updated favicon declarations** in `src/layouts/Layout.astro` to include all required formats
- **Added Apple web app title** meta tag (`<meta name="apple-mobile-web-app-title" content="FiNAN" />`)
- **Updated web app manifest** (`site.webmanifest`) with correct app names:
  - name: "FiNAN - Filipino Nurses Association in the Nordic Region"
  - short_name: "FiNAN"

## Changed

### Favicon File Structure

- **Moved favicon files** from `/public/favicon/` directory to `/public/` root for simpler paths
- **Updated all favicon paths** in Layout.astro to reference root-level files:
  - `/favicon.ico` (instead of `/favicon/favicon.ico`)
  - `/favicon.svg` (new)
  - `/favicon-96x96.png` (new)
  - `/apple-touch-icon.png` (instead of `/favicon/apple-touch-icon.png`)
  - `/site.webmanifest` (instead of `/favicon/site.webmanifest`)

### Web App Manifest Icons

- **Renamed Android Chrome icons** to follow RealFaviconGenerator naming convention:
  - `android-chrome-192x192.png` → `web-app-manifest-192x192.png`
  - `android-chrome-512x512.png` → `web-app-manifest-512x512.png`
- **Added maskable purpose** to web app icons in manifest for better PWA support

## Technical Details

All three issues identified by [RealFaviconGenerator Favicon Checker](https://realfavicongenerator.net/favicon-checker) are now resolved:
- ✅ SVG favicon declared and working
- ✅ 96×96 desktop PNG favicon included
- ✅ Apple mobile web app title meta tag added
- ✅ ICO favicon with proper sizes (16×16, 32×32, 48×48)
- ✅ Apple touch icon (180×180) optimized
- ✅ PWA web app manifest with maskable icons

## Files Modified

- `src/layouts/Layout.astro` - Updated favicon link declarations (lines 76-82)
- `public/site.webmanifest` - Updated app name and icon paths
- All favicon files in `/public/` directory replaced with optimized versions

## Build Status

✅ Project builds successfully with all new favicon files correctly included in production output.
