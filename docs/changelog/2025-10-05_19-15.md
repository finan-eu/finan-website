# Changelog — 2025-10-05 19:15 (Europe/Helsinki)

## Summary

- Added `docs/finan-website-improvement-review.md` to capture prioritized improvement recommendations for the codebase.
- Updated `src/layouts/Layout.astro` mobile viewport helper so the inline script is valid JavaScript and no longer triggers TypeScript-only syntax or IDE warnings.

## Details

- Authored a structured audit report covering high-, medium-, and low-priority issues, with concrete file references and remediation tips.
- Replaced the runtime `ReturnType` annotation in the layout’s viewport-height utility with browser-safe JavaScript (`resizeTimer` defaults to `0` and uses `window.setTimeout`), preventing parse failures while keeping TypeScript satisfied.

## Follow-up

- Review the audit document and open issues or tasks for each recommendation as needed.
- Consider running linting/format checks before committing these changes.
