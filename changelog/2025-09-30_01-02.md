# Changelog - 2025-09-30 01:02

## UI/UX Improvements

### HeroHeader Component (`src/components/HeroHeader.astro`)

- **Improved text formatting and spacing**
  - Increased contact text size from `text-sm` to `text-base` on mobile, and `text-base` to `text-lg` on larger screens for better readability
  - Removed unnecessary horizontal padding (`px-2`) from description and contact paragraphs to improve alignment
  - Enhanced code formatting for better maintainability (multi-line formatting for `set:html` attribute)

### Navbar Component (`src/components/Navbar.astro`)

- **Responsive logo sizing improvements**
  - Reduced navbar height on mobile devices from `h-20` to `h-14` for better space utilization
  - Adjusted logo size: smaller on mobile (`h-9`) and standard size on large screens (`lg:h-12`)
  - Improved responsive behavior across different screen sizes
